"""Parses the contents of the *.gdi file and generates new file contents that are expected by the SD card maker program"""

class GdiParser:
    @staticmethod
    def process(gdi_file):
        return gdi_file